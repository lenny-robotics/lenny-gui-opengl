#version 330 core

layout(location = 0) in vec3 modelPosition;

out vec2 textureCoordinates;

void main(){
	gl_Position =  vec4(modelPosition, 1);
	textureCoordinates = (modelPosition.xy + vec2(1, 1)) / 2.0;
}

